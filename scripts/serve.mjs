import http from 'node:http';
import { readFile, stat } from 'node:fs/promises';
import path from 'node:path';
const root=path.resolve('out');const types={'.html':'text/html; charset=utf-8','.css':'text/css','.js':'text/javascript','.json':'application/json','.txt':'text/plain','.xml':'application/xml','.svg':'image/svg+xml','.png':'image/png','.jpg':'image/jpeg','.webp':'image/webp','.woff2':'font/woff2','.pdf':'application/pdf'};
http.createServer(async(req,res)=>{try{let url=new URL(req.url,'http://localhost');let name=decodeURIComponent(url.pathname);let file=path.resolve(root,'.'+name);if(!file.startsWith(root+path.sep)&&file!==root){res.writeHead(403);res.end();return}let s=await stat(file).catch(()=>null);if(s?.isDirectory()){if(!name.endsWith('/')){res.writeHead(308,{location:name+'/'+url.search});res.end();return}file=path.join(file,'index.html')}let body=await readFile(file).catch(()=>null);if(!body){res.statusCode=404;file=path.join(root,'404.html');body=await readFile(file)}res.setHeader('Content-Type',types[path.extname(file)]??'application/octet-stream');res.setHeader('X-Content-Type-Options','nosniff');res.setHeader('X-Robots-Tag','noindex, follow');res.end(body)}catch{res.writeHead(400);res.end('Bad request')}}).listen(Number(process.env.PORT||3000),'0.0.0.0',()=>console.log('Review server: http://localhost:'+(process.env.PORT||3000)));
